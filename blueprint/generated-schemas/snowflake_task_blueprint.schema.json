{
  "description": "Configuration for a Snowflake Task Blueprint.",
  "properties": {
    "snowflake_conn_id": {
      "default": "snowflake_conn",
      "description": "Airflow connection id used to reach Snowflake.",
      "title": "Snowflake Conn Id",
      "type": "string"
    },
    "snowflake_task": {
      "description": "Fully qualified Snowflake task name in DATABASE.SCHEMA.TASK format.",
      "title": "Snowflake Task",
      "type": "string"
    },
    "poll_interval_seconds": {
      "default": 30,
      "description": "Seconds between TASK_HISTORY checks while deferred.",
      "maximum": 3600,
      "minimum": 5,
      "title": "Poll Interval Seconds",
      "type": "integer"
    },
    "task_timeout_seconds": {
      "default": 3600,
      "description": "Maximum seconds to wait for the Snowflake task.",
      "minimum": 60,
      "title": "Task Timeout Seconds",
      "type": "integer"
    },
    "blueprint": {
      "type": "string",
      "const": "snowflake_task_blueprint"
    },
    "version": {
      "type": "integer",
      "const": 1
    }
  },
  "required": [
    "snowflake_task",
    "blueprint",
    "version"
  ],
  "title": "SnowflakeTaskBlueprint",
  "type": "object",
  "$schema": "http://json-schema.org/draft-07/schema#"
}